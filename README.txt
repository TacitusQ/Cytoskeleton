to run
$ g++ cytoskel.cpp
$ ./a.out

to render
$ make render.run

to manipulate input
   modify file command.txt
   refer to inputModule.h for command references

to save video
   modify render controls in render.cpp
   output goes to default fname which will be OVERWRITTEN
   if the video is important, rename immediately after rendering
